// Payload resolves custom admin components through this map. The multi-tenant plugin's
// TenantField component is registered by the plugin itself, so nothing is needed here yet;
// `pnpm payload generate:importmap` regenerates it when a custom component is added.
export const importMap = {}
