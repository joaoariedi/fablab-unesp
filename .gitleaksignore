# Findings gitleaks has already adjudicated, one fingerprint per line.
#
# A fingerprint is `commit:file:rule:line`, so an entry silences exactly one occurrence in one
# commit — not a file, not a rule, not a path. That precision is the point: allow-listing
# `**/tests/**` would have silenced this one and every real credential a test file ever gains.
#
# ── f288aac, review.test.ts:49 ────────────────────────────────────────────────────────────────
# A fixture password for a user the test creates and the test alone ever logs in as. It was
# flagged by `generic-api-key` on ENTROPY (3.68), not on a pattern: the sibling fixtures use
# `fixture-password-123`, whose repetition scores lower, and were never flagged. The literal is
# now that same shared value, so the finding cannot recur — but the string stays in this commit
# forever, and a scan of the branch's history still reaches it.
f288aac04bf25d761a4d742381d7a6b478bd07aa:apps/web/tests/content/review.test.ts:generic-api-key:49

# ── 25da0c9, signed-in.test.ts:315 ────────────────────────────────────────────────────────────
# The same finding as above, by the same mechanism, in a fixture written a week later: a password
# for a user this test creates and this test alone ever logs in as. `signed-in-vantage-123` was
# flagged by `generic-api-key` on ENTROPY (3.56) — the sibling fixtures all contain the repeated
# token `password`, which scores lower, and none of them has ever been flagged.
#
# The literal is now `fixture-password-123`, the shared value the earlier adjudication settled
# on, so the finding cannot recur. This entry exists only because the string is in that commit
# forever and a scan of the branch's history still reaches it — a fingerprint, not a path rule.
25da0c9047e0168683342d65a1fc78f18818e03c:apps/web/tests/tenancy/signed-in.test.ts:generic-api-key:315

