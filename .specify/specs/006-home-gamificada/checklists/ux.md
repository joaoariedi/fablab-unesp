# Checklist: ux
<!-- Generated by /speckit.checklist -->
<!-- Branch: 006-home-gamificada | Date: 2026-09-16 -->

The states the mockups do not draw. `home.md` draws one populated Home; every row below is a
screen a real lab will show on some day and the art does not cover.

| ID | Check | Dimension | Ref | Status |
|----|-------|-----------|-----|--------|
| CHK046 | The mission card **with no bar** (signed out) versus **with a 0% bar** (signed in, not started): two different cards, and `home.md` draws neither. What does each look like, and does the card keep its height? | [responsiveness] | CLR-002, FR-009 | [ ] |
| CHK047 | The sign-in invitation in the band: is it a link per card, one for the whole band, or text? Three invitations on one row is three calls to action for one act | [feedback] | FR-009, US2 | [ ] |
| CHK048 | The lab card on a lab's **first day**: level 0, an empty bar, `0 / 5 XP`. Does it read as new or as broken? CLR-004 refused these panels precisely because zeroes look like a product nobody uses | [feedback] | FR-015, US8 | [ ] |
| CHK049 | The lab card at the **cap**: level 10, where `progressInLevel` reports a full bar that can never move again. Does the card say something, or does it show a permanently full bar with no explanation? | [feedback] | FR-013, FR-014 | [ ] |
| CHK050 | The ranking card with **fewer than five** makers: does it draw empty rows to keep the card's height, or shrink? The card sits in a three-card row whose heights are visually matched | [responsiveness] | FR-017, US5 | [ ] |
| CHK051 | The ranking card with **zero** makers shows *"Ainda sem makers no ranking"* — is the `VER RANKING COMPLETO` link still there, leading to a board that is also empty? | [feedback] | FR-020, US5 | [ ] |
| CHK052 | **Every maker has a null avatar today** — the compositor is blocked (004 T042, ISS-003). Is the placeholder a frame, an initial, a silhouette? Five identical placeholders in a column is the ranking card's real v1 appearance | [feedback] | FR-019, US5 | [ ] |
| CHK053 | A signed-in maker whose **personal read failed** while the missions loaded: the band renders without percentages, which is exactly what a signed-out visitor sees. Does anything tell them the difference, or do they read it as "I am not signed in"? | [error-states] | FR-010, US1 | [ ] |
| CHK054 | Three cards in a row, one of them in its error state: does the row keep its shape, or does the error card collapse and shift the other two? | [responsiveness] | FR-023, US7 | [ ] |
| CHK055 | `Tentar novamente` reloads the page. If the failure is persistent, pressing it produces the same screen — is there any wording that sets that expectation, or does it look like the button does nothing? | [feedback] | FR-023 | [ ] |
| CHK056 | The three panels on **mobile**: `home.md` stacks the three-card row. Where does the band of three missions go, and is the Home's mobile hero still the LCP element once three panels sit below it? | [responsiveness] | FR-033, US9 | [ ] |
| CHK057 | `ProgressBar` announces `aria-valuetext` as a percentage while the label says the mission's name. For a bar with **no value** (signed out), is the element absent or present-and-empty? A `progressbar` role with no value is announced as indeterminate | [accessibility] | CLR-002, FR-009 | [ ] |
| CHK058 | The ranking's two-digit positions (`01`, `02`) are decorative zero-padding. Does a screen reader announce "zero one" — and should the position be marked up as a list rather than as text? | [accessibility] | FR-018 | [ ] |
| CHK059 | Each panel is a section with a heading. Do the three headings land in the document outline in an order that makes sense after the hero and before `ÚLTIMOS PROJETOS`, and does each error state keep its heading so the page's structure does not change with the data? | [accessibility] | FR-023, US7 | [ ] |
