{
  "name": "@fablab/game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure XP / level / mission rules. No Payload, no IO.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
