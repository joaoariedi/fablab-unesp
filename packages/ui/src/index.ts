/**
 * @fablab/ui — identity tokens and base components.
 *
 * Placeholder in feature 000; feature 001 fills it from the mockups in docs/product/.
 * The binding rule when it does: zero hexadecimal literals in components — every identity
 * value is a CSS custom property resolved from the organization record. See README.md.
 */

export {}
